# AI Developer Instructions for QuadPlot

あなたはシニアフロントエンドエンジニアとして、このプロジェクトの実装をサポートしてください。
以下の要件と制約を厳守してコードを出力・編集してください。

## プロジェクトの目的
あらゆる事象やアイディア、感覚を4象限（十字軸）に直感的にマッピングして視覚化するSPAツール。
コーヒーやワインなどの味覚の記録から、タスクの優先度整理、ポジショニングマップの作成など、特定の基準にとらわれず自由に要素をプロットし、全体を俯瞰するためのアプリケーションです。

## 技術スタックとルール
1. **JavaScript (Vanilla JS)**: TypeScriptは使用せず、ピュアなJavaScriptで記述すること。
2. **Alpine.js**: 状態管理とDOMの操作はAlpine.jsの機能（`x-data`, `x-bind`, `x-on` など）を活用し、シンプルに実装すること。
3. **Tailwind CSS**: スタイリングにはTailwind CSSのユーティリティクラスを使用し、カスタムCSSの記述は最小限に抑えること。
4. **ビルド環境**: Bun + Viteで動作する構成を前提とすること。

## 実装する機能 (MVP)
以下の機能を段階的に実装してください。

1. **ベースレイアウト**: 画面全体に十字の軸（X軸・Y軸）を描画する。
2. **アイテム追加**: 画面上の任意の場所をダブルクリック（または指定の操作）するとプロンプトが表示され、入力したテキストのラベルとドット（点）がその座標に配置される。
3. **ドラッグ＆ドロップ**: 配置されたアイテムをマウスで自由に移動できるようにする。
4. **永続化**: アイテムのデータ（一意のID、テキスト、X座標、Y座標）はAlpine.jsのコンポーネントデータとして管理し、変更があるたびに `localStorage` に保存。初期ロード時に復元する。

## 設計上の制約事項（絶対にやらないこと）
- バックエンドサーバーやデータベースへの通信処理を記述しないこと（すべてローカルで完結させる）。
- 複雑なコンポーネント設計や、過度な抽象化を行わないこと。1〜2つのファイル（`index.html` と `main.js`）でシンプルに見通し良く記述すること。
- モバイル向けの複雑なレスポンシブデザインは考慮しなくてよい（PC向け画面を優先）。
- MVPのスコープ外である「JSONの入出力」や「タブ切り替え」の実装を勝手に追加しないこと。

## 期待する出力
- 指示された機能を満たす、シンプルで動作するコードを提示してください。
- 修正や機能追加を行う際は、既存の動作を壊さないことを確認してから提案してください。